{
  "name": "matchmake-ink-sdk",
  "version": "0.0.4",
  "description": "The node package that serves as a wrapper for the backend to matchmake.ink",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest"
  },
  "keywords": [],
  "author": "firesquid",
  "license": "GPL3.0",
  "devDependencies": {
    "typescript": "^5.1.6",
    "vitest": "^0.33.0"
  },
  "dependencies": {
    "firebase": "^10.1.0"
  }
}
